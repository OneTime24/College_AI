import type { Room } from '../types'
import { PageTitle } from './Dashboard'
import { StatusPill } from '../components/StatusPill'
export function Rooms({ rooms }: { rooms: Room[] }) { return <><PageTitle title="Rooms" text="Monitor connected spaces and their simulated hardware."/><div className="page-grid">{rooms.map(room=><article className="panel room-detail" key={room.id}><div className="panel-head"><div><h2>{room.name}</h2><p>{room.building}, Floor {room.floor} · {room.room_number}</p></div><StatusPill value={room.active_mode}/></div><p className="description">{room.description}</p><dl className="metrics"><div><dt>Temperature</dt><dd>{room.temperature ?? '—'}°C</dd></div><div><dt>Humidity</dt><dd>{room.humidity ?? '—'}%</dd></div><div><dt>Occupancy</dt><dd>{room.occupancy}</dd></div></dl><div className="devices-mini"><span>CONNECTED DEVICES</span>{room.devices.length ? room.devices.map(device=><div key={device.id}><strong>{device.name}</strong><small>{device.device_type}</small><StatusPill value={device.is_online}/></div>) : <p>No devices registered for this room.</p>}</div><p className="simulation-note">SIMULATION — Hardware controls are not connected.</p></article>)}</div></> }
