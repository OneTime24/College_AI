import { PageTitle } from './Dashboard'
export function Placeholder({type}:{type:'assistant'|'settings'}) { const assistant=type==='assistant'; return <><PageTitle title={assistant?'AI Assistant':'Settings'} text={assistant?'Local intelligence roadmap':'System configuration overview'}/><section className="panel placeholder"><h2>{assistant?'Local AI engine will be connected in the next development phase.':'Foundation settings'}</h2><p>{assistant?'This dashboard does not simulate AI answers. The next phase will connect a real local LLM, with college-approved knowledge and offline operation.':'Configuration is currently managed through the backend environment file. Future phases will add role-based administration and operational settings.'}</p></section></> }
