import type { Device, Room } from '../types'
import { PageTitle } from './Dashboard'
import { StatusPill } from '../components/StatusPill'
export function Devices({devices,rooms}:{devices:Device[];rooms:Room[]}) { const roomNames=new Map(rooms.map(r=>[r.id,r.name])); return <><PageTitle title="Devices" text="IoT-ready device registry. Current devices use demonstration simulation data."/><section className="panel table-panel"><div className="panel-head"><div><h2>Device inventory</h2><p>{devices.length} devices in the local database</p></div><span className="simulation-note">SIMULATION</span></div><div className="table-wrap"><table><thead><tr><th>Device</th><th>Type</th><th>Room</th><th>Status</th><th>Connectivity</th></tr></thead><tbody>{devices.map(d=><tr key={d.id}><td><strong>{d.name}</strong></td><td>{d.device_type}</td><td>{roomNames.get(d.room_id) ?? 'Unknown'}</td><td>{d.status}</td><td><StatusPill value={d.is_online}/></td></tr>)}</tbody></table></div></section></> }
